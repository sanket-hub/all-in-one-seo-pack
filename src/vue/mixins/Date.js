import {
	dateSqlToLocal,
	dateSqlToLocalRelative,
	dateJsToLocal,
	dateStringToLocalJs
} from '@/vue/utils/date'

export const Date = {
	methods : {
		dateSqlToLocal,
		dateSqlToLocalRelative,
		dateJsToLocal,
		dateStringToLocalJs
	}
}